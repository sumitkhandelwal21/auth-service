version: "3.8"

services:
  app:
    build: .
    container_name: node-docker-app
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: npm start
